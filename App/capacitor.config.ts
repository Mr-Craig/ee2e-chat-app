import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.acraig.chatapp',
  appName: 'E2EE Chat',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
